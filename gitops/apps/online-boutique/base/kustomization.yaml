# online-boutique.yaml is vendored verbatim from
# GoogleCloudPlatform/microservices-demo (autogenerated upstream, "do not
# manually edit" per its own header) - kept as one file rather than split
# per-resource so future re-vendoring is a straight file replace, not a
# manual re-split. Left as a single Kustomize resource entry for that
# reason.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - online-boutique.yaml
