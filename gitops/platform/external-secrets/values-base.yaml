installCRDs: true

serviceAccount:
  create: false
  name: external-secrets

# Webhook + cert-controller ship enabled by default (chart values) and are
# required for ExternalSecret/ClusterSecretStore admission validation - left
# at defaults rather than disabled, unlike some of the other charts in this
# repo which turn off pieces they don't need.
