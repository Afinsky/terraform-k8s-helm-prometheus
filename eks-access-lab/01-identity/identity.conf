profile        = "terraform"                                 # In what account will keep terraform.tfstate
bucket         = "dev-me-terraform-state"                    # Same bucket as environments/develop
key            = "eks-access-lab/identity/terraform.tfstate"
region         = "us-east-1"                                 # Bucket region (bucket already exists in us-east-1)
use_lockfile   = true
encrypt        = true
